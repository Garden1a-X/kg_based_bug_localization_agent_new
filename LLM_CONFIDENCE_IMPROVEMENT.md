# LLM 置信度评估改进

## 问题描述

从测试结果发现，LLM 在入口选择时过于自信，在信息不足的情况下仍然给出较高的置信度。

### 问题案例

**测试2** - 日志内容：
```
ALL phases bad!
mmc0: tuning execution failed: -1
mmc0: error -1 whilst initialising MMC card
```

**候选入口**：
```python
['dw_mci_pltfm_probe', 'dw_mci_probe', 'mxs_mmc_probe',
 'renesas_sdhi_probe', 'sdhci_pltfm_probe', ...]
```

**问题表现**：
- LLM 选择：`sdhci_pltfm_probe`
- 置信度：**60%**（刚好达到阈值）
- 实际：日志中**没有明确证据**表明是 SDHCI 驱动
- 预期行为：应该置信度 < 60%，触发降级模式

**分析**：
- 日志只提到了通用的 "mmc0"，没有驱动特定信息
- 候选列表中有多个 MMC 驱动（dw_mci, sdhci, mxs_mmc 等）
- LLM 基于某种猜测选择了 sdhci，但这是不可靠的

---

## 改进方案

### 核心原则

**保守评估原则**：
> 宁可置信度低也不要过度自信。只有在有明确证据时才给出高置信度。

### Prompt 修改要点

#### 1. **明确高置信度条件（>= 0.6）**

**必须**满足以下之一：
- ✅ 日志中明确提到了驱动名称
  - 例如："dwmmc", "sdhci", "dw_mci" 等
- ✅ 用户提供了明确的平台或驱动信息
  - 例如：`user_context = {'platform': 'RK3288'}`
- ✅ 日志中包含该驱动的特有函数名
  - 例如："dw_mci_execute_tuning" → 明确是 dw_mci 驱动

#### 2. **明确低置信度情况（< 0.6）**

**必须**给出低置信度的情况：
- ❌ 日志仅提到通用子系统名（如 "mmc", "usb"）
- ❌ 候选列表中有多个驱动都可能匹配
- ❌ 只是基于猜测或经验，没有硬证据
- ❌ 日志信息过于简单，无法确定具体驱动

#### 3. **严格的置信度标准**

```
0.8-1.0: 有明确的硬证据
         - 日志中的驱动名
         - 用户明确指定
         - 特有函数名

0.6-0.8: 有一定证据
         - 如日志中的特有函数可以推断出驱动类型

0.4-0.6: 有弱证据
         - 如基于子系统名猜测，但不确定

0.0-0.4: 无法确定
         - 日志信息太少，无法判断
```

#### 4. **具体示例**

在 prompt 中提供了明确的示例：

| 场景 | 置信度 | 说明 |
|------|--------|------|
| "mmc0: tuning failed" + 多个候选 | **0.3** | 无法确定是哪个驱动 |
| "dw_mci: tuning failed" | **0.9** | 明确提到 dw_mci |
| "mmc0: tuning failed" + platform="RK3288" | **0.8** | RK3288 使用 dw_mci |
| 日志含 "dw_mci_execute_tuning" | **0.8** | 特有函数名 |

#### 5. **强化推理过程**

要求 LLM 明确检查证据：
```json
"reasoning": [
  "推理步骤1：检查日志是否明确提到驱动名 - 未提到",
  "推理步骤2：检查是否有特有函数 - 没有",
  "推理步骤3：候选列表中有多个MMC驱动，无法确定",
  "推理步骤4：结论 - 无法确定具体驱动，confidence=0.3"
]
```

---

## 预期效果对比

### 改进前

```
测试2: 日志解析（带候选入口）
✓ 推断入口: sdhci_pltfm_probe
✓ 置信度: 60%                    ← 问题：刚好达到阈值
✓ 需要更多信息: True

推理：基于MMC相关猜测选择SDHCI  ← 问题：缺乏硬证据
```

### 改进后（预期）

```
测试2: 日志解析（带候选入口）
✓ 推断入口: mmc_attach_mmc        ← 降级到日志最上层函数
✓ 置信度: 30%                     ← 正确：低置信度
✓ 降级模式: True                  ← 正确：触发降级
✓ 需要更多信息: True

推理：
1. 日志未明确提到驱动名 - 仅有"mmc0"
2. 候选中有多个MMC驱动，无法确定
3. 无特有函数可供判断
4. 结论 - confidence=0.3，需要用户提供更多信息
```

### 改进后（有证据的情况）

**场景1**：日志含特有函数
```
日志: "dw_mci_execute_tuning failed: -1"
✓ 推断入口: dw_mci_pltfm_probe
✓ 置信度: 85%                     ← 高置信度（有特有函数名）
✓ 降级模式: False
```

**场景2**：用户提供平台信息
```
日志: "mmc0: tuning failed"
用户上下文: {'platform': 'RK3288'}
✓ 推断入口: dw_mci_pltfm_probe
✓ 置信度: 80%                     ← 高置信度（用户明确了平台）
✓ 降级模式: False
```

---

## 技术实现

### 修改的文件

- `llm/llm_client.py` - `analyze_log()` 方法的 prompt

### 关键代码段

```python
prompt = f"""你是一个Linux内核驱动错误分析专家。

**重要原则：保守评估，宁可置信度低也不要过度自信。**

## 入口选择和置信度评估（非常重要！）

**只有满足以下条件之一时，才能给出 confidence >= 0.6：**
1. ✅ 日志中明确提到了驱动名称
2. ✅ 用户提供了明确的平台或驱动信息
3. ✅ 日志中包含该驱动的特有函数名

**以下情况必须给出 confidence < 0.6：**
1. ❌ 日志仅提到通用的子系统名（如 "mmc", "usb"）
2. ❌ 候选列表中有多个驱动都可能匹配
3. ❌ 只是基于猜测或经验，没有硬证据
...
"""
```

---

## 测试建议

### 测试用例

#### 用例1：低置信度场景（应触发降级）
```python
log = "mmc0: tuning execution failed: -1"
candidates = ['dw_mci_probe', 'sdhci_probe', 'mxs_mmc_probe']
user_context = None

# 预期：confidence < 0.6，降级模式
```

#### 用例2：高置信度场景（特有函数）
```python
log = "dw_mci_execute_tuning: tuning failed"
candidates = ['dw_mci_probe', 'sdhci_probe']
user_context = None

# 预期：confidence >= 0.8，选择 dw_mci_probe
```

#### 用例3：高置信度场景（用户上下文）
```python
log = "mmc0: tuning failed"
candidates = ['dw_mci_probe', 'sdhci_probe']
user_context = {'platform': 'RK3288', 'driver_hint': 'Designware MMC'}

# 预期：confidence >= 0.8，选择 dw_mci_probe
```

### 运行测试

```bash
# 在 venv 环境中运行
python test_entry_point_selection.py
```

**检查点**：
1. 测试2 的置信度应该 < 60%
2. 应该看到降级模式被触发
3. 应该看到建议提供的信息列表

---

## 影响范围

### 直接影响
- LLM 在信息不足时会更保守，更多触发降级模式
- 降级模式下仍能提供部分调用链（日志函数之间的路径）

### 用户体验
**改进前**：
- 可能给出错误的入口，导致搜索不到正确的调用链
- 用户不知道为什么找不到路径

**改进后**：
- 明确告知用户信息不足
- 提供具体的建议（需要平台信息、驱动类型等）
- 降级模式仍能提供有价值的部分调用链
- 用户可以补充信息后重新运行，获得更准确的结果

### 优势
1. **更可靠**：避免基于猜测的错误选择
2. **更透明**：明确告知用户为什么需要更多信息
3. **有保障**：降级模式确保总能提供一些结果
4. **可交互**：引导用户提供正确的信息

---

## 总结

### 核心改进
✅ 从"过度自信"转变为"保守谨慎"
✅ 明确定义了高/低置信度的判断标准
✅ 提供具体示例指导 LLM
✅ 强化推理过程要求

### 效果
- ❌ 减少错误的入口选择
- ✅ 增加降级模式的使用（这是好事）
- ✅ 提高用户信任度（透明的置信度）
- ✅ 引导用户提供正确信息

### 后续
- 收集真实使用数据，持续优化阈值
- 可能需要微调置信度阈值（当前是 0.6）
- 考虑添加多候选尝试机制
